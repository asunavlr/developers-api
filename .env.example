## Exemplo de variáveis de ambiente
# Copie este arquivo para ".env" e preencha com seus valores reais.
# O arquivo ".env" já está listado no .gitignore e não deve ser commitado.

# URL do seu projeto Supabase (Dashboard → Settings → API)
SUPABASE_URL=https://<SEU-PROJETO>.supabase.co

# Chave pública (anon), usada para operações autenticadas pelo usuário
SUPABASE_ANON_KEY=<SUA-ANON-KEY>

# (Opcional, recomendado em desenvolvimento/local)
# Chave Service Role para operações administrativas (bypass de RLS)
SUPABASE_SERVICE_ROLE_KEY=<SUA-SERVICE-ROLE-KEY>

# Porta padrão da API
PORT=3000